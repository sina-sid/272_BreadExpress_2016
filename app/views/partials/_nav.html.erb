<!-- TO DO:
      - Restructure navbar for admin
      - Restructure navbar for customer
      - Add User details page (for customer, this is a special version of customers, for employees, this is new)
      - Add links for baker and shipper 
-->

<div class="contain-to-grid">
  <nav class="top-bar" data-topbar>
    <section class="top-bar-section">
      <ul class="left">
        <!-- link to home -->
        <li><%= link_to "Home", home_path %></li>
        <% if !logged_in? %>
          <li><%= link_to "View Menu", items_path %></li>
        <% end %>
        <% if logged_in? %>
          <!-- options for orders -->
          <li class="has-dropdown">
            <%= link_to "Orders", orders_path %>
            <ul class="dropdown">
              <li><%= link_to "View Orders", orders_path %></li>
              <% if current_user.role?(:admin) %>
                <li><%= link_to "New Order", new_order_path %></li>
              <% end %>
            </ul>
          </li>

          <!-- options for addresses -->
          <li class="has-dropdown">
            <%= link_to "Addresses", addresses_path %>
            <ul class="dropdown">
              <li><%= link_to "View Addresses", addresses_path %></li>
              <% if current_user.role?(:admin) %>
                <li><%= link_to "Add an Address", new_address_path %></li>
              <% end %>
            </ul>
          </li>

          <!-- options for customers -->
          <% if !current_user.role?(:customer) %>
            <li class="has-dropdown">
              <%= link_to "Customer", customers_path %>
              <ul class="dropdown">
                <li><%= link_to "All Customers", customers_path %></li>
                <% if current_user.role?(:admin) %>
                  <li><%= link_to "Add a Customer", new_customer_path %></li>
                <% end %>
              </ul>
            </li>
          <% end %>

          <!-- options for items -->
          <li class="has-dropdown">
            <%= link_to "Item", items_path %>
            <ul class="dropdown">
              <li><%= link_to "All Items", items_path %></li>
              <% if current_user.role?(:admin) %>
                <li><%= link_to "Add an Item", new_item_path %></li>
              <% end %>
            </ul>
          </li>

          <!-- cart -->
          <% if current_user.role?(:customer) %>
            <li><%= link_to "Cart", cart_path %></li>
          <% end %>

          <!-- options for users -->
          <li class="has-dropdown">
            <% if !current_user.role?(:customer) %>
              <%= link_to "Welcome #{current_user.username}!", user_path(current_user) %>
              <ul class="dropdown">
              <li><%= link_to "Your Profile", user_path(current_user) %></li>
            <% else %>
              <%= link_to "Welcome #{current_user.customer.first_name}!", user_path(current_user) %>
              <ul class="dropdown">
              <li><%= link_to "Your Profile", user_path(current_user) %></li>
            <% end %>
            <li><%= link_to "Logout", logout_path %></li>
            </ul>
            
          </li>




        <% end %>

        <!-- ADMIN ONLY OPTIONS -->









    </section>
  </nav>
</div>